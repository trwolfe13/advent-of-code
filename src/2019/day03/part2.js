const wires = require('./wires');
const string = require('../../util/string');

module.exports = function (input) {
  // const [wire1, wire2] = string.lines(input)
  //   .map(w => wires.parse(w))
  //   .map(i => wires.expand(i));
  // const intersections = wires.intersections(wire1, wire2);
  // return wires.fastest(intersections);
  return 'Too slow';
}