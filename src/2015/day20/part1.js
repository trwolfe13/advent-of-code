const elves = require('./elves');

module.exports = function (input) {
  // const target = Number(input), skip = 200;
  // let x = elves.firstHouse(target, 0, skip);
  // x -= skip;
  // return elves.firstHouse(target, x, 1);
}